We assume data to be in folder './data' and in csv formats.

To train, first install dependencies, then, run `python3 train.py`

To predict, run `python3 predict.py`

Other approaches, visualisations are in the `others` folder.
No guarantees that they would work or run again due to innumerous changes and updates made.
